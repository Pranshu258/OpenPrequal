# Dockerfile for OpenPrequal Backend
FROM python:3.11-slim

WORKDIR /app

COPY src/ src/
COPY requirements.txt ./

RUN pip install --no-cache-dir -r requirements.txt
RUN mkdir -p /app/logs

EXPOSE 8000

ENV PYTHONPATH=src
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]
