apiVersion: v1
kind: Service
metadata:
  name: openprequal-backend
spec:
  type: {{ .Values.backend.service.type }}
  selector:
    app: openprequal-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 8001
